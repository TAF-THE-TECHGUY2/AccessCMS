export async function saveOnboardingDraft(onboardingData) {
  void onboardingData;
  return Promise.resolve({ ok: true });
}

export async function submitOnboardingProfile(profileData) {
  void profileData;
  return Promise.resolve({ ok: true });
}

export async function finalizeOnboarding(onboardingData) {
  void onboardingData;
  return Promise.resolve({ ok: true });
}
